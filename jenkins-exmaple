pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Hello World'
            }
        }
     stage ('Build'){
      steps {
         echo 'hi here im'
       }
    }
   stage ('deploy') {
       steps {
         echo 'hi world'
       }
    }
  } 
 }
